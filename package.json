{
  "name": "tropoatlas",
  "version": "2.0.0",
  "description": "TropoAtlas Workspace",
  "homepage": "https://tropoatlas.esaracco.fr",
  "workspaces": [
    "apps/*",
    "packages/*",
    "plugins/*/*"
  ],
  "author": "Emmanuel Saracco",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "audit": "npm audit --omit=dev --audit-level=high",
    "dev:audio": "npm run dev -w apps/tropoaudio",
    "dev:cine": "npm run dev -w apps/tropocine",
    "build:audio": "npm run build -w apps/tropoaudio",
    "build:cine": "npm run build -w apps/tropocine",
    "build": "npm run build:audio && npm run build:cine",
    "test": "vitest run",
    "lint": "./node_modules/.bin/eslint apps/ packages/ plugins/",
    "lint-fix": "./node_modules/.bin/eslint --fix apps/ packages/ plugins/",
    "format": "./node_modules/.bin/prettier --write apps/ packages/ plugins/",
    "format:check": "./node_modules/.bin/prettier --check apps/ packages/ plugins/",
    "i18n:check": "node scripts/check_i18n.js",
    "pre-commit": "npm run lint && npm run format:check && npm run test && npm run i18n:check",
    "prepare": "husky"
  },
  "devDependencies": {
    "@eslint/js": "^9.20.0",
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "eslint": "^9.20.0",
    "eslint-plugin-react": "^7.37.5",
    "husky": "^9.1.7",
    "jsdom": "^24.1.3",
    "prettier": "^3.9.6",
    "vitest": "^3.2.6"
  }
}
